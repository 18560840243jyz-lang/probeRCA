# Final ProbeRCA-BPF ??? / ?????

## ??

?????????????????????????????????????????????????????? Healthy?Soft?Hard?Burst ? Recovery ???? `proberca.orchestration.engine` ??????????????????????????

```mermaid
flowchart LR
  C["Collectors: service / host / directed edge / Burst"] --> W["CollectedWindow"]
  W --> S["sealed collection archive"]
  S --> V["integrity and contract verification"]
  V --> AS["Healthy masked RLS As"]
  AS --> SOFT["Soft: freeze baseline, As and topology"]
  SOFT --> AV["healthy-only masked Ridge Av"]
  AV --> HARD["Hard + following Burst window"]
  HARD --> R["signed cross-metric residual, root coordinates only"]
  R --> F["non-negative Sparse-Group FISTA"]
  F --> O["Top-K entity + root category + direct evidence"]
```

## ????

- `proberca/dataplane/` ?????????????????????? write-once ??????????
- `proberca/controlplane/` ??????????? Collector????????
- `proberca/cli/seal_collection.py` ?????????
- `proberca/cli/analyze_collection.py` ?????????
- `proberca/dataplane/adapters.py` ??? `EngineWindowInput` ??????? `CollectedWindow`????????????

## ???????

??????????????????????????????????

- ??????? `scope=service` ?? 9 ??????3 ???/?????? CPU(2)?Memory?IO?Lock?LocalNet ?????
- ?????????? `scope=node` ?? CPU PSI?Memory PSI?IO PSI?NIC drop/error ? 4 ????
- ????? TCP ? DNS ?????? `scope=service_pair` ?? count?latency P95?failure ? 3 ????
- Burst ??? `source_type=burst_event` ????????????????
- `root_*`?`target_*`?`injected_path`?`ground_truth` ??????????????????
- ???????????????????????????? cluster?

??????

- `collected-windows.jsonl`??????????
- `collection-manifest.json`???????????????? SHA-256?????? `sealed=true`?

?? manifest?????????????????????????????????????????

## ???????

1. Healthy ???? median/MAD ????????????? `z`?????? `max(z, 0)`?
2. ?? Healthy ??????????? RLS `As`??????Flow?????????????????
3. Soft ??????`As` ?????????? L2 ???????????/???????????????
4. ???????????????????????????????? Soft ??? Healthy ???? Ridge `Av`?
5. `Av` ?????????????????????????????FISTA ????????????
6. Hard ?????????? Burst ???Burst ?? noisy-OR ?? `H_c`??? `lambda_grp_eff=lambda_grp_0/(1+eta*H_c)` ??????????
7. ???? Sparse-Group FISTA ????????????????????? L2 ??????????????????? FISTA?

## ????

????? `CollectedWindow.create(...)` ???????????? `from_engine_window(...)` ???????

```bash
python3 -m proberca.cli.seal_collection \
  --windows-jsonl /path/to/collected-windows.input.jsonl \
  --collection-contract configs/final_collection_contract.yaml \
  --dataset-id RUN_ID \
  --source-description 'final scheme collection' \
  --output /path/to/sealed-archive
```

????????? `phase=collection_sealed` ????????

```bash
python3 -m proberca.cli.analyze_collection \
  --archive /path/to/sealed-archive \
  --config configs/final_control.yaml \
  --output /path/to/control-output
```

????? `control-run.json` ? `rca-results.jsonl`?????????????????????????????????????Burst ?? ID/????????????FISTA ????????????
